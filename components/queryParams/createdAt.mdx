import Parameter from "../../components/parameter";

<Parameter name="createdAt[X]" type="date" required={false}>
  Limit results to records created before or after the value provided. See the
  documentation for [Date](/entities/date) for more details.

  <Parameter name="X" in="path" type="'after' | 'before'" required={true}>
    Whether to filter records created after or before the value provided.

    * `after` — Only records created after the value provided will be returned.
    * `before` — Only records created before the value provided will be
      returned.

  </Parameter>

</Parameter>
