import Parameter from '../../components/parameter';

<Parameter 
  name='page[after]'
  type='number'
  required={false}
>
  Return the page after the cursor provided. Cursors are found within the [page data](#pagedata) payload.
</Parameter>