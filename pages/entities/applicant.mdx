import Applicant from "../../components/pageContent/entities/applicant.mdx";
import ExampleContent from "../../components/pageContent/endpoints/applications/exampleData.mdx";

# Applicant

Applications (and the applicants within them) are created when a user applies for tickets that are set up to require an application process, such as balloted or "Good For Age" entries. Each applicant represents a single person who has applied for a ticket.

<ExampleContent />

<Applicant />
