import { Tab, Tabs } from "nextra-theme-docs";

import Applicant from "../../components/pageContent/entities/applicant.mdx";
import PageData from "../../components/pageContent/entities/pageData.mdx";
import DefaultQueryParameters from "../../components/queryParams";
import Authorization from "../../components/headers/authorization.mdx";

import ExampleContent from "../../components/pageContent/endpoints/applications/exampleData.mdx";

# Applications

The Applications endpoint returns a paged list of all applications for a balloted event.

## Paths for "Applications"

- `/v0/applications` — Returns all applications that you have access to.
- `/v0/events/:event_id/applications` — Returns all applications for a specific event.

<Tabs items={["Request", "Response"]}>
  {/* <!-- prettier-ignore --> */}
  <Tab>
    ```bash filename="Example"
    curl --request GET \
      --url "https://api.letsdothis.com/v0/applications" \
      --header "Authorization: Bearer YOUR-TOKEN"
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

## Parameters for "Applications"

### Headers

<Authorization />

### Query Parameters

<DefaultQueryParameters />

### Response

<Tabs items={['Shape', 'Example']}>
  <Tab>
    ```
    {
      data: Applicant[],
      page: PageData | undefined
    }
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

#### Response Types

##### [Applicant](/entities/applicant)

<Applicant />

##### [PageData](/entities/pageData)

<PageData />
