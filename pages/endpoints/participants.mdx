import { Tab, Tabs } from "nextra-theme-docs";

import PageData from "../../components/pageContent/entities/pageData.mdx";
import Participant from "../../components/pageContent/entities/participant.mdx";
import DefaultQueryParameters from "../../components/queryParams";
import Authorization from "../../components/headers/authorization.mdx";

import ExampleContent from "../../components/pageContent/endpoints/participants/exampleData.mdx";

# Participants

The Participants endpoints returns a paged list of all participants.

## Paths for "Participants"

- `/v0/participants` — Returns all participants that you have access to.
- `/v0/races/:race_id/participants` — Returns all participants for a specific race.

<Tabs items={["Request", "Response"]}>
  {/* <!-- prettier-ignore --> */}
  <Tab>
    ```bash filename="Example"
    curl --request GET \
      --url "https://api.letsdothis.com/v0/participants" \
      --header "Authorization: Bearer YOUR-API-KEY"
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

## Parameters for "Participants"

### Headers

<Authorization />

### Query Parameters

<DefaultQueryParameters />

### Response

<Tabs items={['Shape', 'Example']}>
  <Tab>
    ```
    {
      data: Participant[],
      page: PageData
    }
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

#### Response Types

##### [Participant](/entities/participant)

<Participant />

##### [PageData](/entities/pageData)

<PageData />
