import { Tab, Tabs } from "nextra-theme-docs";

import PageData from "../../components/pageContent/entities/pageData.mdx";
import Parameter from "../../components/parameter";
import Participant from "../../components/pageContent/entities/participant.mdx";
import DefaultQueryParameters from "../../components/queryParams";

import ExampleContent from "../../components/pageContent/endpoints/participants/exampleData.mdx";

# Participants

The Participants endpoint returns a paged list of all participants for all events for a given Event Organizer.

<Tabs items={["Request", "Response"]}>
  {/* <!-- prettier-ignore --> */}
  <Tab>
    ```bash filename="Example"
      https://api.letsdothis.com/v0/participants
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

## Parameters for "Participants"

### Headers

<Parameter name="Authorization" type="string" required={true}>
  An Authorization header is required to access this resource. See the
  [Authentication docs](/gettingStarted/authentication) for more information.
</Parameter>

### Query Parameters

<DefaultQueryParameters />

### Response

<Tabs items={['Shape', 'Example']}>
  <Tab>
    ```
    {
      data: Participant[],
      page: PageData | undefined
    }
    ```
  </Tab>
  <Tab>
    <ExampleContent />
  </Tab>
</Tabs>

#### Response Types

##### [Participant](/entities/participant)

<Participant />

##### [PageData](/entities/pageData)

<PageData />
